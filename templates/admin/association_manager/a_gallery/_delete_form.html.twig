<form method="post" action="{{ path('app_a_gallery_delete', {'id': a_gallery.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ a_gallery.id) }}">
    <button class="btn">Delete</button>
</form>
